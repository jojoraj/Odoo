<odoo>
  <data>
        <record id="menu_action_boat" model="ir.actions.act_window">
            <field name="name">Bateau</field>
            <field name="type">ir.actions.act_window</field>
            <field name="domain">[('is_boat','=',True)]</field>
            <field name="context">{'default_is_boat': True}</field>
            <field name="res_model">fleet.vehicle</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                <!-- Add Text Here -->
            </p><p>
                <!-- More details about what a user can do with this object will be OK -->
            </p>
            </field>
        </record>

        <record id="menu_action_vehicule" model="ir.actions.act_window">
            <field name="name">Véhicule</field>
            <field name="type">ir.actions.act_window</field>
            <field name="domain">[('is_boat','=',False)]</field>
            <field name="context">{'default_is_boat': False}</field>
            <field name="res_model">fleet.vehicle</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                <!-- Add Text Here -->
            </p><p>
                <!-- More details about what a user can do with this object will be OK -->
            </p>
            </field>
        </record>

        <menuitem
            id="boat_menu_id"
            name="Bateau"
            action="menu_action_boat"
            sequence="25"
            parent="fleet.menu_root"/>

        <menuitem
            id="fleet.fleet_vehicles"
            name="Véhicule"
            action="menu_action_vehicule"
            sequence="2"
            parent="fleet.menu_root"/>
  </data>
</odoo>
